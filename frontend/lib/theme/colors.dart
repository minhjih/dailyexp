import 'package:flutter/material.dart';

// 메인 컬러 - 트렌디한 인디고 블루
const primaryColor = Color(0xFF4F46E5); // 밝은 인디고
const primaryLightColor = Color(0xFF818CF8); // 연한 인디고
const secondaryColor = Color(0xFF6366F1); // 보조 인디고

// 중립 컬러
const backgroundColor = Color(0xFFF8FAFC); // 매우 연한 그레이
const surfaceColor = Colors.white;
const surfaceVariantColor = Color(0xFFF1F5F9); // 연한 슬레이트

// 텍스트 컬러
const primaryTextColor = Color(0xFF1E293B); // 진한 슬레이트
const secondaryTextColor = Color(0xFF64748B); // 중간 슬레이트
const tertiaryTextColor = Color(0xFF94A3B8); // 연한 슬레이트

// 액센트 컬러
const successColor = Color(0xFF10B981); // 에메랄드
const warningColor = Color(0xFFF59E0B); // 앰버
const errorColor = Color(0xFFEF4444); // 레드
const infoColor = Color(0xFF3B82F6); // 블루

// 상태 컬러
const activeColor = Color(0xFF4F46E5); // 활성 상태
const inactiveColor = Color(0xFFE2E8F0); // 비활성 상태

// 그라데이션 컬러
const gradientStart = Color(0xFF4F46E5); // 인디고
const gradientEnd = Color(0xFF818CF8); // 연한 인디고
